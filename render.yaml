services:
  - type: web
    name: tecnoenlace
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tecnoenlace-db
          property: connectionString

databases:
  - name: tecnoenlace-db